require "spec_helper"

RSpec.describe Matrix::Ruby::Sdk do
  it "has a version number" do
    expect(Matrix::Ruby::Sdk::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
